
server:
  port: 8082 #${PORT:0}
spring:
  application:
    name: DebitCard-Movement
  main:
    allow-bean-definition-overriding: true
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: CreditMovement

eureka:
  client:
  service-url:
  defaultZone: http://localhost:8761/eureka